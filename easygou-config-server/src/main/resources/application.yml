server:
  port: 4000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka
  instance:
    prefer-ip-address: true
spring:
  application:
    name: config-server #服务名
  cloud:
    config:
      server:
        git:
          uri: https://github.com/app0ll0/easygou-config.git
          username: app0ll0
          password: oy327324252oyl